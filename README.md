# Create python environment and install pre-requisites
pip install mujoco gymnasium numpy torch stable-baselines3 tensorboard

# Visualise nugus xml
To view the xml file, run inside activated environment:
python -m mujoco.viewer --mjcf=nugus/nugus.xml 

